server.port=${PORT:8080}

# Optional comma-separated CORS origin patterns (e.g. https://your-web-host.com) in addition to dev + Capacitor defaults.
qswarm.cors.extra-origins=${QSWARM_CORS_EXTRA_ORIGINS:}

